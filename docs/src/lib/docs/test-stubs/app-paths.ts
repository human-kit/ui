// Test stub for `$app/paths`. The docs run with the default (relative) paths, so
// `resolve` is an identity for the absolute `/docs/...` routes the components pass.
export const resolve = (path: string) => path;
export const base = '';
export const assets = '';
