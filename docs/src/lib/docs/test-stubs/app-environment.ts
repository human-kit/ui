// Test stub for `$app/environment` (SvelteKit is not loaded under vitest). Tests
// run in a real browser, so `browser` is true.
export const browser = true;
export const dev = true;
export const building = false;
export const version = 'test';
