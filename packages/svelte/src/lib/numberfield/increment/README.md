# NumberField.Increment

Stepper button that increases the current value by `step`.

It renders a native button through `ButtonRoot`, uses `incrementAriaLabel` from Root, and disables itself when the field is disabled, read-only, or already at `max`.
