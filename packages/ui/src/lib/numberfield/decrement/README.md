# NumberField.Decrement

Stepper button that decreases the current value by `step`.

It renders a native button through `ButtonRoot`, uses `decrementAriaLabel` from Root, and disables itself when the field is disabled, read-only, or already at `min`.
