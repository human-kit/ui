# NumberField.Input

Renders the visible text input with `role="spinbutton"`.

The input displays the formatted value, accepts localized numeric text, and updates the root `value` as `number | null`. Provide an accessible name with `aria-label`, `aria-labelledby`, or an associated label.
