.svelte-kit
dist
build
node_modules

# Generated lockfile
pnpm-lock.yaml

# Vendored agent skill references (managed via skills-lock.json)
.agents/

# Generated by changesets (prerelease state); the tool writes 2-space JSON,
# which conflicts with useTabs and breaks lint after every version bump.
.changeset/pre.json

# Svelte-flavored markdown docs content: prettier's markdown parser mangles
# inline Svelte expressions ({...}) — demo .svelte files and api.json stay formatted
docs/src/content/**/*.md
